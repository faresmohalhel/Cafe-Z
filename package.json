{
  "name": "cafe-z",
  "version": "1.0.0",
  "description": "a webpage for a cafe called Z",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwindcss -i ./input.css -o ./src/style.css",
    "watch": "tailwindcss -i ./input.css -o ./src/style.css --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.2.7"
  }
}
